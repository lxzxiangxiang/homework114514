# Checklist

- [x] `AgarClone_Qt/` 目录存在，包含全部 14 个任务中声明的文件
- [x] CMakeLists.txt 配置正确：C++17、Qt6 Core+Widgets、所有源文件已列出
- [x] `cmake -S AgarClone_Qt -B AgarClone_Qt/build` 配置无错误
- [x] `cmake --build AgarClone_Qt/build` 编译通过，0 error 0 warning
- [x] Constants.h 包含文档需求 2 中所有游戏常量定义
- [x] Entity.h 声明 EntityType 枚举、虚方法 update/onEaten、纯虚方法 entityType
- [x] Ball.h 声明所有成员变量（vx, vy, isPlayer, aiLevel, aiId, splitTimer, mergeTimer, invincibleTimer, skill, skillTimer, debuff, debuffTimer, lastDx, lastDy）
- [x] Ball.h 声明 SkillType 枚举（Speed/Shield/Grow/Invisible/Magnet）和 DebuffType 枚举（Bomb/Trap/Poison）
- [x] Ball.h 声明 move/split/eject/eat/applySkill/applyDebuff/isInvincible/hasShield 方法
- [x] Food.h / Food.cpp 实现 Entity 派生，构造函数随机半径 3~8
- [x] SkillBall.h / SkillBall.cpp 实现 Entity 派生，包含 skillType 和 lifetime
- [x] Hazard.h / Hazard.cpp 实现 Entity 派生，包含 HazardType 枚举和 lifetime
- [x] EjectBall.h / EjectBall.cpp 实现 Entity 派生，包含 vx, vy, lifetime
- [x] SpatialGrid.h / SpatialGrid.cpp 实现 clear/add/getNearby，使用 QHash 存储
- [x] AIController.h / AIController.cpp 声明 AIState 结构体和 updateAI 静态方法
- [x] GameScene.h / GameScene.cpp 声明所有实体列表、updateGame 方法、SpatialGrid 成员
- [x] GameView.h / GameView.cpp 声明 State 枚举、QTimer、keyPressEvent、updateCamera
- [x] UIManager.h / UIManager.cpp 声明 HUD 元素、showMenu/showPause/showGameOver/showVictory/updateHUD
- [x] main.cpp 创建 QApplication 和 GameView，窗口标题 "Agar.io Clone"
- [x] 所有 .h 文件有 `#pragma once` 或 include guard
- [x] 所有 .cpp 文件 `#include` 对应的 .h 文件
